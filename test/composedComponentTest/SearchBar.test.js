import React from "react";
import { create } from "react-test-renderer";
import SearchBar from "../../app/src/composedComponent/SearchBar";

describe("test SearchBar component", () => {

	test("test if Select and Input components exist", (done) => {
//		const testSearchBar = create().toJSON();
//
//		expect(testSearchBar.type).toBe("div");
//		expect(testSearchBar.children.length).toBe(2);
		done();
	});
});
